The web application with all function(Add Task, Delete Task, Update Task, Login, Logout).
The web application use the perbared statment in all fields.
The web application has validation for all fields.
The web application with design.
Problem
the web application design is not stable for all screens.
  